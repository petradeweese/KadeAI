positioning:
  default_position_size_usd: 15000
  target_return_pct:
    min: 15
    max: 20
  max_hold_minutes: 30

risk_limits:
  max_trades_per_day: 5
  daily_loss_limit_usd: 3000

invalidation:
  mode: reclaim
  timeframe: 1m
  require_qqq_confirmation: true
